name: Run Postman Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      environment:
        description: 'Select Postman Environment'
        required: true
        default: 'localhost.postman_environment.json'
        type: choice
        options:
          - localhost.postman_environment.json
          - otro_entorno.postman_environment.json
      branch:
        description: 'Select Branch'
        required: true
        default: 'main'
        type: string

jobs:
  test:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js (Newman)
        uses: actions/setup-node@v4
        with:
          node-version: '16'

      - name: Install dependencies
        run: |
          npm install -g newman
          npm install -g newman-reporter-htmlextra

      - name: Run Postman tests
        continue-on-error: true  # Permite que los tests sigan ejecutándose aunque fallen
        run: |
          cd src/test/springboot-test-postman  # Cambiar al directorio donde está el archivo .bat
          cmd.exe /c run_test.bat  # Ejecutar el .bat

      - name: Upload Test Report
        uses: actions/upload-artifact@v4
        with:
          name: postman-report
          path: src/test/springboot-test-postman/reporte_*.html  # Asegurarse de que el archivo HTML se está subiendo correctamente

  deploy-report:
    needs: test  # Esperar a que el job de tests termine
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download Postman report artifact
        uses: actions/download-artifact@v4
        with:
          name: postman-report

      # Verificar el contenido del artefacto para asegurar que el archivo HTML está disponible
      - name: List downloaded files
        run: |
          ls postman-report/

      - name: Move HTML file to 'reportes' folder
        run: |
          mkdir -p reportes  # Crear la carpeta 'reportes' si no existe
          mv postman-report/reporte_*.html reportes/  # Mover el archivo HTML a la carpeta 'reportes'

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: reportes  # Publicar solo la carpeta 'reportes'
          destination_dir: .  # Opcional, todo en la raíz de gh-pages
          publish_branch: gh-pages

      - name: Get last report filename
        id: get_filename
        run: |
          echo "reportFileName=$(ls reportes/reporte_*.html | tail -n 1)" >> $GITHUB_ENV

      - name: Add link to report in GitHub Actions Summary
        uses: actions/github-script@v7
        with:
          script: |
            const filename = process.env.reportFileName;
            const link = `https://SEBASZC.github.io/springbootweb-restapi/reportes/${filename}`;
            core.summary
              .addHeading('🔗 Reporte de pruebas')
              .addLink('Ver reporte Postman', link)
              .write();